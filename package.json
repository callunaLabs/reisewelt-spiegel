{
  "name": "reisewelt-spiegel",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "tinacms dev -c \"astro dev --host 0.0.0.0\"",
    "build": "tinacms build --skip-cloud-checks && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "@tinacms/cli": "^2.1.11",
    "astro": "^6.0.8",
    "tailwindcss": "^4.2.2",
    "tinacms": "^3.6.3"
  }
}
